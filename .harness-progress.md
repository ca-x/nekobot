# Harness 特性移植进度

## 已完成 ✅

1. **Audit Log 系统** - `pkg/audit/`
   - 工具调用审计日志，存储在 `.nekobot/audit.jsonl`
   - 提交：`c409cf1`

2. **Streaming Bash** - `pkg/tools/streaming.go`
   - 实时命令输出流
   - 提交：`c409cf1`

3. **配置结构** - `pkg/config/config.go`
   - 新增 Audit, Undo, Preprocess, Learnings, Watch 配置
   - 提交：`583245d`

4. **前端配置页面** - `pkg/webui/frontend/src/pages/ConfigPage.tsx`
   - 已添加新配置 sections
   - 提交：`583245d`

5. **Learnings JSONL 系统** - `pkg/memory/`
   - 持久化学习记录，带时间衰减压缩
   - 集成到 Agent 和 Context Builder
   - 提交：`46026ac`

6. **Turn Undo 系统** - `pkg/session/snapshot.go`, `pkg/tools/undo.go`
   - 每轮对话前保存快照
   - 支持撤销到上一轮状态
   - JSONL 存储在 `.nekobot/sessions/`
   - 提交：`580741d`

7. **@file Mentions 预处理** - `pkg/preprocess/`
   - 解析 @file 和 @dir 引用
   - 自动注入文件内容到 prompt
   - 支持大文件截断和缓存
   - 提交：`580741d`

8. **Watch Mode** - `pkg/watch/`
   - 文件系统监控 (fsnotify)
   - Glob 模式匹配
   - Debounce 防抖
   - 命令执行和审计日志
   - 提交：`580741d`

## Git 提交历史

```
580741d feat: add Turn Undo, @file Mentions, and Watch Mode harness features
46026ac feat: add Learnings JSONL system for durable context compression
583245d feat(webui): add harness config sections to ConfigPage
c409cf1 feat: add audit log and streaming bash from yoyo-evolve harness
7355624 fix(webui): polish second-round UX details
```

## 配置示例

```json
{
  "undo": { "enabled": true, "max_turns": 20 },
  "learnings": { "enabled": true, "max_raw_entries": 500 },
  "preprocess": { "enabled": true, "max_file_size": 102400 },
  "watch": {
    "enabled": true,
    "debounce_ms": 300,
    "patterns": [
      { "file_glob": "**/*.go", "command": "go test ./..." }
    ]
  }
}
```

services:
  nekobot:
    image: czyt/nekobot:latest
    container_name: nekobot
    restart: unless-stopped
    ports:
      - "18790:18790" # gateway
      - "18791:18791" # webui (default)
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./data:/app/data
